name: Reconocimiento temporal 5 (navegador real)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  recon-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install playwright@1.48.0
      - run: npx playwright install --with-deps chromium
      - run: node scripts/scraper/recon-partido.mjs > recon-browser-output.txt 2>&1 || true
      - name: Subir salida
        run: |
          git config user.name "recon-bot"
          git config user.email "recon-bot@users.noreply.github.com"
          git add recon-browser-output.txt
          git commit -m "Reconocimiento: salida del navegador real (temporal)" || echo "sin cambios"
          git push

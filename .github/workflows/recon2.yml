name: Reconocimiento temporal 2 (jornada 1 + goleadores)

on:
  workflow_dispatch:

jobs:
  recon2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/scraper/recon3.mjs

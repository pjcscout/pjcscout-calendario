name: Reconocimiento jornada 3

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  recon-jornada3:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install playwright@1.48.0
      - run: npx playwright install --with-deps chromium
      - run: JORNADA=3 node scripts/scraper/scrape-jornada-ffcv.mjs > recon-j3-ffcv-output.txt 2>&1 || true
      - run: JORNADA=3 node scripts/scraper/scrape-jornada-dh7.mjs > recon-j3-dh7-output.txt 2>&1 || true
      - name: Subir resultados
        run: |
          git config user.name "recon-bot"
          git config user.email "recon-bot@users.noreply.github.com"
          git add recon-j3-ffcv-output.txt recon-j3-dh7-output.txt jornada3-detalle.json jornada3-dh7-actas.json
          git commit -m "Reconocimiento: detalle de jornada 3 (temporal)" || echo "sin cambios"
          git push

name: Reconocimiento horarios jornada 4

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  recon-horarios:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/scraper/recon-horarios-j4.mjs
      - name: Subir resultado
        run: |
          git config user.name "recon-bot"
          git config user.email "recon-bot@users.noreply.github.com"
          git add recon-horarios-j4.json
          git commit -m "Reconocimiento: respuesta cruda de horarios jornada 4 (temporal)" || echo "sin cambios"
          git push
